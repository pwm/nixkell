# Nixkell config

greet = "Nixkell"

[env]
ghc = "8.10.3"
packages = [
  "cabal-install",
  "haskell-language-server",
  "haskellPackages.implicit-hie",
  "hlint",
  "niv",
  "ormolu",
]

[ignore]
files = [
  "README.md",
]
paths = [
  ".github",
  ".vscode",
]
